# @sui402/indexer

Sui402 event normalization, stores, and indexer CLI for session-spend and settlement events.

```bash
npm install @sui402/indexer
```

The package also exposes the `sui402-indexer` CLI for syncing chain events into Postgres or a console HTTP sink.
