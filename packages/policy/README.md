# @sui402/policy

Policy helpers for constraining Sui402 payment kinds, networks, amounts, coin types, and resource scopes.

```bash
npm install @sui402/policy
```

Use this package to fail closed when a challenge or proof is outside the limits your application is willing to accept.
