# @sui402/gateway

Gateway primitives for Sui402 merchant configuration and protected service routing.

```bash
npm install @sui402/gateway
```

Use this package when building a gateway that fronts one or more paid services.
