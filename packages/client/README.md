# @sui402/client

Agent-side SDK for Sui402 payments. It discovers Sui402 provider manifests, handles HTTP 402 challenges, submits Sui payments or session spends, and retries the protected request.

```bash
npm install @sui402/client
```

Use this package in agents, wallets, and API clients that need to consume Sui402-protected services.
