# @sui402/server

Express middleware and provider helpers for protecting HTTP routes with Sui402 payments.

```bash
npm install @sui402/server
```

Use this package when you operate an API and want to require Sui402 one-shot or session payments before serving protected resources.
