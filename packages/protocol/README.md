# @sui402/protocol

Core Sui402 protocol primitives: payment challenges, proofs, manifests, canonical IDs, and shared validation helpers.

```bash
npm install @sui402/protocol
```

Use this package when implementing Sui402-compatible clients, servers, gateways, or protocol tooling.
