# @sui402/registry

Registry routes and types for publishing Sui402 paid service listings.

```bash
npm install @sui402/registry
```

Use this package to expose discoverable service metadata for agents and wallets.
