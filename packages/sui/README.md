# @sui402/sui

Sui-specific transaction builders, proof verifiers, payment-session helpers, and provider discovery support for Sui402.

```bash
npm install @sui402/sui
```

Most applications should start with `@sui402/client` or `@sui402/server`; use this package directly when you need lower-level Sui primitives.
